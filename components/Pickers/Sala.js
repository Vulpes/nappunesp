import React, { Component } from 'react';
import {
    View,
    Picker,
} from 'react-native';

export default class Sala extends Component {
    render() {
        return(
            <Picker/>
        );
    }
}