import React, { Component } from 'react';
import {
    View,
    Picker,
} from 'react-native';

export default class Piso extends Component {
    render() {
        return(
            <Picker/>
        );
    }
}